.slideContent    .slideWraper        .subMenu#submenu            .slideTitle 功能导航            ul.menuitems#menuitems    script.        getmenu(1);        function getmenu(type) {            $.post('/api/getmenu', {actmod: "2"}, function (data) {                var json = data;                if (json.length > 0) {                    BuildMenu(json);                }            });        }        function BuildMenu(pjson) {            var TitleLi;            var SubMenuli;            for (var i = 0; i < pjson.length; i++) {                var actUrl = pjson[i].currentMenu.acturl;                var actSubMenu = pjson[i].subMenu;                if (actSubMenu !== null) {                    TitleLi = '<li class="item"><div class="micon"></div><a href="javascript:;" class="pock">' + pjson[i].currentMenu.actname + '</a>'                        + '<ul class="itemsub"></ul></li>';                    $('#menuitems').append(TitleLi);                    for (j in actSubMenu) {                        SubMenuli = '<li><a href="' + pjson[i].subMenu[j].currentMenu.acturl + '">' + pjson[i].subMenu[j].currentMenu.actname + '</a></li>';                        $('.item').eq(i).find('.itemsub').append(SubMenuli);                    }                } else {                    TitleLi = '<li class="item"><div class="micon"></div><a href="' + actUrl + '" class="pock">' + pjson[i].currentMenu.actname + '</a>'                        + '</li>';                    $('#menuitems').append(TitleLi);                }            }            buildMenuList();        }    script(src='/js/subMenu.js').